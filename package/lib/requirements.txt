# Installed into output/<app>/lib by ucc-gen. Required by UCC Configuration REST
# handlers (AdminExternalHandler). Without this, Splunk Web shows
# "Unable to xml-parse the following data: %s" because the handler traceback is
# not Atom XML.
#
# Pin solnlib below 8: 8.x pulls grpcio/OpenTelemetry wheels that do not match
# Splunk's Python.
# splunktaucclib >=8.2.0 required by ucc-gen 6.x (used by the UCC App Builder).
splunktaucclib>=8.2.0,<9
solnlib>=5.5.0,<8
