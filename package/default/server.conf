# Splunk merges app-level server.conf at install. trustedIP lets the persist
# REST handler obtain trusted sessions for KV store I/O (workspace ACL still
# enforced in Python services).
[general]
trustedIP = 127.0.0.1
