[install]
is_configured = 0
state = enabled
build = 1

[package]
id = dev_settings
check_for_updates = false

[launcher]
author = STIG in Splunk
description = Development-only Splunk Web settings that disable splunkd static asset caching. Do not enable on production indexers or search heads serving end users.
version = 1.0.0

[ui]
is_visible = false
label = Dev settings

[id]
version = 1.0.0
name = dev-settings

[triggers]
reload.web = simple
